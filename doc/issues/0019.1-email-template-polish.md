---
id: 0019.1
title: email template polish — Subject lines + branded HTML + drop unsubscribe block (5 transactional templates)
status: open
priority: medium (chassis-completion; required for Primary inbox placement; authentication infrastructure already verified by 0019)
found_by: 0019 verification 2026-04-25 — change-email verification email landed in Gmail spam folder despite all auth checks (`dkim=pass`, `spf=pass`, `dmarc=pass`). Root causes are all template content: empty Subject, unprocessed ASM unsubscribe template variables (`<%asm_group_unsubscribe_raw_url%>` rendering as literal text), minimal body, cold domain reputation compounding.
---

## Context

0019 landed the authentication infrastructure (SPF, DKIM, DMARC, Cloudflare Email Routing for `dmarc@`). All three authentication checks pass on live mail. **But mail still goes to spam** because the SendGrid Dynamic Templates are still in their initial-stub state from 0010 ("real copy lands with the consumer tickets"):

- **Empty Subject lines** — the strongest single spam signal in Gmail.
- **Unprocessed ASM (Advanced Suppression Manager) variables** rendering as literal `<%asm_group_unsubscribe_raw_url%>` in the body — looks like template-leaking, a phishing artifact to mail filters.
- **Minimal body content** — `<p>Full name </p><p>Change URL ...</p>` looks template-leaky.

This ticket polishes the templates so authenticated mail reaches the Primary inbox. **No code changes, no DNS changes** — pure SendGrid Dashboard edits.

### Why transactional templates don't need the unsubscribe block

Per **CAN-SPAM Act**, transactional emails (verification, password reset, security canaries, receipts) are **exempt** from the unsubscribe-link requirement. Marketing emails need it; transactional doesn't. Removing the unsubscribe block from each template both fixes the spam-trigger issue AND is regulatory-correct for chassis-default templates. (If a future ticket adds marketing emails, those would need a separate ASM unsubscribe group.)

## Scope

For each of the 5 Dynamic Templates in SendGrid:

1. **Set the Subject line** on the active version (table below).
2. **Replace the HTML body** with the polished version (each template's section below).
3. **Replace the plain-text body** (each template's section below).
4. **Remove the unsubscribe block** entirely (don't include the `<%asm_group_unsubscribe_raw_url%>` block in the new body).
5. **Save and activate the version.**

Then re-run the verification test from 0019 §Scope item 5 Option B (change-email flow) and confirm Primary inbox placement.

### Subject lines (one-line summary)

| Template (SendGrid name) | Settings field | Subject |
|---|---|---|
| `carddroper-verify-email` | `SENDGRID_TEMPLATE_VERIFY_EMAIL` | `Confirm your email address` |
| `carddroper-reset-password` | `SENDGRID_TEMPLATE_RESET_PASSWORD` | `Reset your password` |
| `carddroper-change-email` | `SENDGRID_TEMPLATE_CHANGE_EMAIL` | `Confirm your new email address` |
| `carddroper-email-changed` | `SENDGRID_TEMPLATE_EMAIL_CHANGED` | `Your email address was changed` |
| `carddroper-credits-purchased` | `SENDGRID_TEMPLATE_CREDITS_PURCHASED` | `Your Carddroper receipt` (deferred — see §Out of scope item 4) |

## Design system (used in all 5 templates)

- **Layout:** centered card on neutral background; `<table>`-based for Outlook compatibility.
- **Card width:** 560px max-width.
- **Body bg:** `#f5f5f7`. **Card bg:** `#ffffff`. **Card radius:** 12px.
- **Primary text:** `#1d1d1f`. **Muted text:** `#86868b`. **Accent (button + links):** `#0071e3`.
- **Font stack:** `-apple-system, BlinkMacSystemFont, 'Segoe UI', Helvetica, Arial, sans-serif`.
- **Inline CSS only** (most email clients strip `<style>` blocks).
- **No images** (text-only is fine; avoids broken-image-on-load placement penalty + alt-text requirements).
- **Brand mark** is a simple uppercase wordmark in muted color above each heading (no logo image).

Adopters cloning the chassis substitute "Carddroper" with their own brand via search-replace in each template body.

---

## Template 1 — `carddroper-verify-email`

**Subject:** `Confirm your email address`

**Variables expected** (from `email_service.send_email`):
- `verify_url` — `${FRONTEND_BASE_URL}/verify-email?token=...`
- `full_name` — optional; nullable

**HTML body:**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Confirm your email address</title>
</head>
<body style="margin:0; padding:0; background:#f5f5f7; font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Helvetica,Arial,sans-serif; color:#1d1d1f;">
  <table role="presentation" width="100%" cellpadding="0" cellspacing="0" border="0" style="background:#f5f5f7;">
    <tr>
      <td align="center" style="padding:32px 16px;">
        <table role="presentation" width="560" cellpadding="0" cellspacing="0" border="0" style="max-width:560px; background:#ffffff; border-radius:12px;">
          <tr>
            <td style="padding:40px;">
              <p style="margin:0 0 24px 0; font-size:13px; color:#86868b; letter-spacing:1px; text-transform:uppercase; font-weight:600;">Carddroper</p>
              <h1 style="margin:0 0 16px 0; font-size:24px; font-weight:600; color:#1d1d1f;">Confirm your email address</h1>
              <p style="margin:0 0 16px 0; font-size:15px; line-height:1.5;">{{#if full_name}}Hi {{full_name}},{{else}}Hi there,{{/if}}</p>
              <p style="margin:0 0 24px 0; font-size:15px; line-height:1.5;">Thanks for signing up. Please confirm your email address by clicking the button below.</p>
              <table role="presentation" cellpadding="0" cellspacing="0" border="0" style="margin:0 0 24px 0;">
                <tr>
                  <td style="background:#0071e3; border-radius:8px;">
                    <a href="{{verify_url}}" style="display:inline-block; padding:12px 24px; color:#ffffff; text-decoration:none; font-size:15px; font-weight:600;">Confirm email</a>
                  </td>
                </tr>
              </table>
              <p style="margin:0 0 8px 0; font-size:13px; line-height:1.5; color:#86868b;">If the button doesn't work, copy and paste this link into your browser:</p>
              <p style="margin:0 0 24px 0; font-size:13px; line-height:1.5; word-break:break-all;"><a href="{{verify_url}}" style="color:#0071e3; text-decoration:underline;">{{verify_url}}</a></p>
              <p style="margin:0; font-size:13px; line-height:1.5; color:#86868b;">If you didn't sign up for Carddroper, you can safely ignore this email.</p>
            </td>
          </tr>
        </table>
      </td>
    </tr>
  </table>
</body>
</html>
```

**Plain text body:**

```text
Carddroper — Confirm your email address

{{#if full_name}}Hi {{full_name}},{{else}}Hi there,{{/if}}

Thanks for signing up. Please confirm your email address by visiting the link below:

{{verify_url}}

If you didn't sign up for Carddroper, you can safely ignore this email.
```

---

## Template 2 — `carddroper-reset-password`

**Subject:** `Reset your password`

**Variables expected:**
- `reset_url` — `${FRONTEND_BASE_URL}/reset-password?token=...`
- `full_name` — optional; nullable

**HTML body:**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Reset your password</title>
</head>
<body style="margin:0; padding:0; background:#f5f5f7; font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Helvetica,Arial,sans-serif; color:#1d1d1f;">
  <table role="presentation" width="100%" cellpadding="0" cellspacing="0" border="0" style="background:#f5f5f7;">
    <tr>
      <td align="center" style="padding:32px 16px;">
        <table role="presentation" width="560" cellpadding="0" cellspacing="0" border="0" style="max-width:560px; background:#ffffff; border-radius:12px;">
          <tr>
            <td style="padding:40px;">
              <p style="margin:0 0 24px 0; font-size:13px; color:#86868b; letter-spacing:1px; text-transform:uppercase; font-weight:600;">Carddroper</p>
              <h1 style="margin:0 0 16px 0; font-size:24px; font-weight:600; color:#1d1d1f;">Reset your password</h1>
              <p style="margin:0 0 16px 0; font-size:15px; line-height:1.5;">{{#if full_name}}Hi {{full_name}},{{else}}Hi there,{{/if}}</p>
              <p style="margin:0 0 24px 0; font-size:15px; line-height:1.5;">We received a request to reset the password for your Carddroper account. Click the button below to choose a new password.</p>
              <table role="presentation" cellpadding="0" cellspacing="0" border="0" style="margin:0 0 24px 0;">
                <tr>
                  <td style="background:#0071e3; border-radius:8px;">
                    <a href="{{reset_url}}" style="display:inline-block; padding:12px 24px; color:#ffffff; text-decoration:none; font-size:15px; font-weight:600;">Reset password</a>
                  </td>
                </tr>
              </table>
              <p style="margin:0 0 8px 0; font-size:13px; line-height:1.5; color:#86868b;">If the button doesn't work, copy and paste this link into your browser:</p>
              <p style="margin:0 0 24px 0; font-size:13px; line-height:1.5; word-break:break-all;"><a href="{{reset_url}}" style="color:#0071e3; text-decoration:underline;">{{reset_url}}</a></p>
              <p style="margin:0 0 8px 0; font-size:13px; line-height:1.5; color:#86868b;">This link will expire in 1 hour.</p>
              <p style="margin:0; font-size:13px; line-height:1.5; color:#86868b;">If you didn't request a password reset, you can safely ignore this email — your password won't be changed.</p>
            </td>
          </tr>
        </table>
      </td>
    </tr>
  </table>
</body>
</html>
```

**Plain text body:**

```text
Carddroper — Reset your password

{{#if full_name}}Hi {{full_name}},{{else}}Hi there,{{/if}}

We received a request to reset the password for your Carddroper account. Visit the link below to choose a new password:

{{reset_url}}

This link will expire in 1 hour.

If you didn't request a password reset, you can safely ignore this email — your password won't be changed.
```

---

## Template 3 — `carddroper-change-email`

**Subject:** `Confirm your new email address`

**Variables expected:**
- `change_url` — `${FRONTEND_BASE_URL}/confirm-email-change?token=...`
- `full_name` — optional; nullable
- `new_email` — destination address (the new email being verified)

**HTML body:**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Confirm your new email address</title>
</head>
<body style="margin:0; padding:0; background:#f5f5f7; font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Helvetica,Arial,sans-serif; color:#1d1d1f;">
  <table role="presentation" width="100%" cellpadding="0" cellspacing="0" border="0" style="background:#f5f5f7;">
    <tr>
      <td align="center" style="padding:32px 16px;">
        <table role="presentation" width="560" cellpadding="0" cellspacing="0" border="0" style="max-width:560px; background:#ffffff; border-radius:12px;">
          <tr>
            <td style="padding:40px;">
              <p style="margin:0 0 24px 0; font-size:13px; color:#86868b; letter-spacing:1px; text-transform:uppercase; font-weight:600;">Carddroper</p>
              <h1 style="margin:0 0 16px 0; font-size:24px; font-weight:600; color:#1d1d1f;">Confirm your new email address</h1>
              <p style="margin:0 0 16px 0; font-size:15px; line-height:1.5;">{{#if full_name}}Hi {{full_name}},{{else}}Hi there,{{/if}}</p>
              <p style="margin:0 0 24px 0; font-size:15px; line-height:1.5;">You requested to change your Carddroper email address to <strong>{{new_email}}</strong>. Click the button below to confirm.</p>
              <table role="presentation" cellpadding="0" cellspacing="0" border="0" style="margin:0 0 24px 0;">
                <tr>
                  <td style="background:#0071e3; border-radius:8px;">
                    <a href="{{change_url}}" style="display:inline-block; padding:12px 24px; color:#ffffff; text-decoration:none; font-size:15px; font-weight:600;">Confirm new email</a>
                  </td>
                </tr>
              </table>
              <p style="margin:0 0 8px 0; font-size:13px; line-height:1.5; color:#86868b;">If the button doesn't work, copy and paste this link into your browser:</p>
              <p style="margin:0 0 24px 0; font-size:13px; line-height:1.5; word-break:break-all;"><a href="{{change_url}}" style="color:#0071e3; text-decoration:underline;">{{change_url}}</a></p>
              <p style="margin:0 0 8px 0; font-size:13px; line-height:1.5; color:#86868b;">This link will expire in 1 hour.</p>
              <p style="margin:0; font-size:13px; line-height:1.5; color:#86868b;">If you didn't request this change, you can safely ignore this email — your email won't be changed.</p>
            </td>
          </tr>
        </table>
      </td>
    </tr>
  </table>
</body>
</html>
```

**Plain text body:**

```text
Carddroper — Confirm your new email address

{{#if full_name}}Hi {{full_name}},{{else}}Hi there,{{/if}}

You requested to change your Carddroper email address to {{new_email}}. Visit the link below to confirm:

{{change_url}}

This link will expire in 1 hour.

If you didn't request this change, you can safely ignore this email — your email won't be changed.
```

---

## Template 4 — `carddroper-email-changed` (THE SECURITY CANARY)

**Subject:** `Your email address was changed`

This is the highest-stakes template. It's the silent-account-takeover canary from 0017 — the only signal the original account owner gets if an attacker briefly compromises an account and changes the email. The Subject must be visible without opening; the body must make recovery affordances obvious; and there must be **no clickable token** (this is a notification, not an action).

**Variables expected:**
- `old_email` — the previous email (where this notification is being sent)
- `new_email` — the new email it was changed to
- `change_date` — ISO-8601 UTC timestamp from `datetime.now(timezone.utc).isoformat()`
- `support_email` — currently `support@carddroper.com` (constant, per 0010)

**HTML body:**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Your email address was changed</title>
</head>
<body style="margin:0; padding:0; background:#f5f5f7; font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Helvetica,Arial,sans-serif; color:#1d1d1f;">
  <table role="presentation" width="100%" cellpadding="0" cellspacing="0" border="0" style="background:#f5f5f7;">
    <tr>
      <td align="center" style="padding:32px 16px;">
        <table role="presentation" width="560" cellpadding="0" cellspacing="0" border="0" style="max-width:560px; background:#ffffff; border-radius:12px;">
          <tr>
            <td style="padding:40px;">
              <p style="margin:0 0 24px 0; font-size:13px; color:#86868b; letter-spacing:1px; text-transform:uppercase; font-weight:600;">Carddroper — Security notice</p>
              <h1 style="margin:0 0 16px 0; font-size:24px; font-weight:600; color:#1d1d1f;">Your email address was changed</h1>
              <p style="margin:0 0 24px 0; font-size:15px; line-height:1.5;">The email address on your Carddroper account was changed:</p>
              <table role="presentation" cellpadding="0" cellspacing="0" border="0" width="100%" style="margin:0 0 24px 0; background:#f5f5f7; border-radius:8px;">
                <tr>
                  <td style="padding:16px 20px; font-size:14px; line-height:1.6;">
                    <strong style="color:#86868b; font-weight:600;">From:</strong> <span style="color:#1d1d1f;">{{old_email}}</span><br>
                    <strong style="color:#86868b; font-weight:600;">To:</strong> <span style="color:#1d1d1f;">{{new_email}}</span><br>
                    <strong style="color:#86868b; font-weight:600;">When:</strong> <span style="color:#1d1d1f;">{{change_date}}</span>
                  </td>
                </tr>
              </table>
              <p style="margin:0 0 8px 0; font-size:15px; line-height:1.5; color:#1d1d1f;"><strong>If this was you</strong>, no action is needed.</p>
              <p style="margin:0 0 24px 0; font-size:15px; line-height:1.5; color:#1d1d1f;"><strong>If this wasn't you</strong>, your account may have been compromised. Contact us immediately at <a href="mailto:{{support_email}}" style="color:#0071e3; text-decoration:underline;">{{support_email}}</a> and reset your password.</p>
              <p style="margin:0; font-size:13px; line-height:1.5; color:#86868b;">This notification was sent to your previous email address as a security measure. You will not receive future Carddroper email at this address.</p>
            </td>
          </tr>
        </table>
      </td>
    </tr>
  </table>
</body>
</html>
```

**Plain text body:**

```text
Carddroper — Security notice

YOUR EMAIL ADDRESS WAS CHANGED

The email address on your Carddroper account was changed:

  From: {{old_email}}
  To:   {{new_email}}
  When: {{change_date}}

If this was you, no action is needed.

If this wasn't you, your account may have been compromised. Contact us immediately at {{support_email}} and reset your password.

This notification was sent to your previous email address as a security measure. You will not receive future Carddroper email at this address.
```

**No clickable token in this email.** No "this wasn't me" button — recovery is via the support email + a separate password-reset flow. This is intentional: an account-takeover canary should not contain any link that could be intercepted or misused.

---

## Template 5 — `carddroper-credits-purchased` (deferred — see §Out of scope)

**Subject:** `Your Carddroper receipt`

The chassis does not currently send the receipt email — the 0023 PAYG topup handler grants credits but doesn't trigger `EMAIL_TEMPLATE.CREDITS_PURCHASED`. Wiring the send + defining the dynamic template variables is a separate consumer ticket (likely under 0023 or a future receipt ticket).

For now, set the Subject only and leave the body as a minimal placeholder. Polish lands when the send is wired.

**Placeholder HTML body** (Subject set, body trivial):

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Your Carddroper receipt</title>
</head>
<body style="margin:0; padding:0; background:#f5f5f7; font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Helvetica,Arial,sans-serif; color:#1d1d1f;">
  <table role="presentation" width="100%" cellpadding="0" cellspacing="0" border="0">
    <tr><td align="center" style="padding:32px 16px;">
      <table role="presentation" width="560" cellpadding="0" cellspacing="0" border="0" style="max-width:560px; background:#ffffff; border-radius:12px;">
        <tr><td style="padding:40px;">
          <p style="margin:0 0 24px 0; font-size:13px; color:#86868b; letter-spacing:1px; text-transform:uppercase; font-weight:600;">Carddroper</p>
          <h1 style="margin:0 0 16px 0; font-size:24px; font-weight:600;">Receipt template — placeholder</h1>
          <p style="margin:0; font-size:15px; line-height:1.5; color:#86868b;">This template will be polished when the receipt-send is wired (separate ticket).</p>
        </td></tr>
      </table>
    </td></tr>
  </table>
</body>
</html>
```

**Plain text body:**

```text
Carddroper — Receipt template placeholder
(Polish lands when the receipt-send is wired.)
```

## Verification

After updating all 5 templates in SendGrid:

1. **Mark prior spam-folder test emails as "Not Spam"** in your Gmail to seed reputation. This is a signal Gmail uses for future emails from this domain.
2. **Re-run the change-email flow** on staging (per 0019 §Scope item 5 Option B):
   - Log in to `https://staging.carddroper.com`.
   - ProfileMenu → Change email → submit.
   - **Both emails** (verification at new address + canary at old address) should now:
     - Land in **Primary inbox** (not Spam, not Promotions).
     - Show a real Subject line (not blank).
     - Render branded HTML with the new design system.
     - Have no `<%asm_group_unsubscribe_raw_url%>` template-leak text.
     - Continue to pass `dkim=pass spf=pass dmarc=pass` (auth carries over from 0019; templates don't affect signing).
3. **Optional reputation check:** send a test verify-email to `mail-tester.com` and aim for 9+/10.

If still in spam after fixing templates: the cold-domain-reputation factor is the remaining contributor. Reputation builds with consistent, well-formed sends + engagement signals over days/weeks. Mark each test email as "Not Spam" until placement is consistent.

## Out of scope

1. **DNS or chassis code changes** — 0019 covered DNS; templates are pure SendGrid Dashboard work.
2. **Marketing email templates** — adding marketing email surface is a future ticket; would need its own ASM unsubscribe group per CAN-SPAM.
3. **Branded HTML refinements beyond the chassis-default design system** — adopters substituting their brand can iterate on copy/colors/logo image as their own polish pass.
4. **`carddroper-credits-purchased` body polish** — the receipt-send is not wired yet (0023 PAYG topup handler does not currently call `send_email(EMAIL_TEMPLATE.CREDITS_PURCHASED, ...)`). Polish lands with the receipt-send wiring ticket. For now, Subject is set + body is a placeholder.
5. **Search-replace of "Carddroper" for adopters** — adopters cloning the chassis substitute "Carddroper" with their own brand in each template manually. The chassis ships templates with the Carddroper brand for consistency with `FROM_NAME`.
6. **Backend send-path changes** — backend continues to pass `dynamic_template_data` per 0010's contract; only the templates' rendering side changes.

## Acceptance

1. All 5 templates have a Subject line set (table above).
2. 4 of 5 templates have the polished HTML + plain-text bodies (CREDITS_PURCHASED stays placeholder per §Out of scope).
3. ASM unsubscribe block (`<%asm_group_unsubscribe_raw_url%>`) is removed from all 5 templates.
4. Re-run change-email flow on staging: both emails land in Primary inbox, render polished HTML, retain `dkim=pass spf=pass dmarc=pass`.
5. Resolution captures the verification result (Primary inbox screenshot or confirmation, mail-tester score if run).

## Verification (re-test)

- `staging.carddroper.com` → log in → ProfileMenu → Change email → submit.
- Both delivered emails: Primary inbox, Subject visible, HTML rendered.
- Optional: `mail-tester.com` 9+/10 score.

## Resolution

*(filled in by orchestrator after user updates templates + re-tests)*
