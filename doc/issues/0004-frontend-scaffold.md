---
id: 0004
title: frontend scaffold — Next.js 16 App Router + TS strict + Tailwind v4 + React Query
status: resolved
priority: high
found_by: orchestrator 2026-04-19
resolved_at: 2026-04-19
---

## Context

PLAN.md §10 item 2. The `/frontend` tree does not exist yet. This ticket creates the bones — no features, no auth pages, no Stripe — so later tickets can layer work on a clean stack. Matches the architecture locked in `doc/architecture/tech-stack.md` (Next.js 15 + React 19 + TypeScript 5 strict + TailwindCSS + React Query v5).

Pattern source for `lib/api.ts` and the eventual auth provider: `/Users/johnxing/mini/foodapp/frontend`. Port the *shape* of those files only — no foodapp-specific features.

## Acceptance

1. Initialize a Next.js 15 project at `/Users/johnxing/mini/postapp/frontend` with:
   - TypeScript, ESLint, Tailwind, App Router enabled.
   - No `src/` wrapper — code lives under `frontend/app/`, `frontend/components/`, `frontend/lib/`, `frontend/hooks/`, `frontend/public/`.
   - `tsconfig.json` has `"strict": true`. No `any` anywhere in the scaffolded code.
2. Package manager: **npm**. `package-lock.json` committed.
3. Tailwind wired via `app/globals.css` with base/components/utilities layers. Default Next.js starter branding removed.
4. Install `@tanstack/react-query` v5 and `@tanstack/react-query-devtools`. Create `app/providers.tsx` as a client component that wraps children in `QueryClientProvider`. Root `app/layout.tsx` imports and uses it.
5. `app/page.tsx` — server component, renders a single `<h1>Carddroper</h1>`. Nothing else. This is the smoke test that the stack is wired.
6. Create `lib/api.ts` with:
   - `API_BASE_URL` read from `process.env.NEXT_PUBLIC_API_BASE_URL`.
   - A typed `apiFetch<T>(path, init?)` helper that sends `credentials: "include"`, sets `Content-Type: application/json` on JSON bodies, throws a typed error on non-OK responses using the backend shape `{ error: { code: string; message: string } }`, and returns `T` on success.
   - No endpoint wrappers yet. Just the helper + error type.
7. Create `frontend/.env.example` with `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000`.
8. `package.json` scripts: `dev`, `build`, `start`, `lint`, `typecheck` (maps to `tsc --noEmit`).
9. Verify:
   - `npm install` completes clean.
   - `npx tsc --noEmit` passes with zero errors.
   - `npm run lint` passes with zero warnings.
   - `npm run dev` starts on port 3000 and the homepage renders "Carddroper" with no console errors.

## Out of scope

- Auth pages (login, register, verify-email, forgot-password, reset-password) — separate ticket.
- Auth context provider and `/auth/me` query — separate ticket.
- Stripe Elements integration — Phase 2 ticket.
- docker-compose integration — separate ticket.
- Test framework setup (Vitest + RTL + Playwright) — comes with the first test-adding ticket.
- Any `README.md` for the frontend — skip.

## Report

- Files created (tree listing is fine).
- Exact installed versions: Next.js, React, TypeScript, Tailwind, `@tanstack/react-query`.
- Any dependency added beyond the ones named above, with a one-line reason.
- Any deviation from the acceptance list.

## Resolution

Scaffold landed at `frontend/` with: Next.js 16.2.4, React 19.2.4, TypeScript 5 strict, Tailwind v4, `@tanstack/react-query` v5.99.2 (+ devtools). `app/providers.tsx` wraps children in `QueryClientProvider` (default `staleTime: 30_000`, devtools dev-only). `app/page.tsx` renders the `<h1>Carddroper</h1>` smoke test. `lib/api.ts` ships the typed `apiFetch<T>` helper with `credentials: "include"` and a typed `ApiError` parsing the backend `{ error: { code, message } }` shape. `frontend/.env.example` written with `NEXT_PUBLIC_API_BASE_URL=http://localhost:8000`. `package.json` scripts: `dev`, `build`, `start`, `lint` (`eslint .`), `typecheck`. `tsc --noEmit` clean, lint clean, dev server up on port 3000.

**Version uplifts vs original ticket:** Next.js 15 → 16 and Tailwind 3 → 4 — `create-next-app` installs latest; orchestrator approved keeping current-stable for a greenfield install. PLAN.md §4 and `doc/architecture/tech-stack.md` updated to lock in Next 16 + Tailwind v4. `next lint` is gone in Next 16; `eslint .` against the generated flat config is the replacement.

**Cleanup:** `frontend/AGENTS.md` + `frontend/README.md` (auto-generated by `create-next-app`) deleted in a follow-up dispatch; tsc + lint still clean.
